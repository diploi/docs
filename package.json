{
  "name": "docs.diploi.com",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host --port 80",
    "start": "if [ ${STAGE} = \"production\" ]; then astro preview --host --port 80; else astro dev --host --port 80; fi",
    "build": "astro check && astro build",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.3.0",
    "@astrojs/starlight": "^0.12.0",
    "astro": "^3.2.3",
    "sharp": "^0.32.5",
    "typescript": "^5.2.2"
  }
}
